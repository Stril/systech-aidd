# 📋 План разработки LLM Telegram-бота

## 📊 Отчет по прогрессу

| Итерация | Функционал | Статус | Дата завершения | Результат |
|----------|-----------|--------|-----------------|-----------|
| **#1** | Базовая инфраструктура + Telegram-бот | ⏳ Ожидание | - | - |
| **#2** | Интеграция с LLM | ⏳ Ожидание | - | - |
| **#3** | Управление контекстом | ⏳ Ожидание | - | - |
| **#4** | In-memory хранилище | ⏳ Ожидание | - | - |
| **#5** | Логирование и обработка ошибок | ⏳ Ожидание | - | - |
| **#6** | Полировка и оптимизация | ⏳ Ожидание | - | - |

**Легенда статусов:**  
⏳ Ожидание | 🔄 В работе | ✅ Завершено | ⚠️ Проблемы | ❌ Отменено

---

## 🚀 Итерации разработки

### Итерация #1: Базовая инфраструктура + Минимальный Telegram-бот
**Цель:** Настроить окружение и запустить бота с базовыми командами

**Задачи:**
- [ ] Создать структуру проекта (`src/`, `tests/`, `logs/`, `docs/`)
- [ ] Настроить `pyproject.toml` с зависимостями
- [ ] Создать `Makefile` с командами `run`, `test`, `install`
- [ ] Создать `.env.example` с примерами переменных
- [ ] Реализовать `Settings` класс с валидацией через pydantic
- [ ] Добавить `.gitignore`
- [ ] Реализовать `TelegramBot` класс (инициализация, запуск polling)
- [ ] Реализовать базовый `MessageHandler` для команд
- [ ] Добавить обработчики `/start` и `/help`
- [ ] Создать `main.py` с точкой входа
- [ ] Добавить базовое логирование в консоль
- [ ] Написать тесты для `Settings` класса
- [ ] Написать тесты для `MessageHandler` (команды `/start`, `/help`)
- [ ] Запустить тесты: `make test` - все должны пройти

**Тест:** Бот запускается через `make run`, отвечает на `/start` приветствием и на `/help` списком команд

---

### Итерация #2: Интеграция с LLM
**Цель:** Бот отправляет сообщения в LLM и возвращает ответы

**Задачи:**
- [ ] Реализовать `OpenAIClient` класс для работы с OpenRouter
- [ ] Добавить метод `send_message()` для отправки запросов к LLM
- [ ] Подключить обработку текстовых сообщений в `MessageHandler`
- [ ] Добавить базовый системный промпт
- [ ] Обработать простые ошибки API
- [ ] Написать тесты для `OpenAIClient` (mock API запросы)
- [ ] Написать тесты для обработки текстовых сообщений
- [ ] Запустить тесты: `make test` - все должны пройти

**Тест:** Бот получает текстовое сообщение от пользователя, отправляет в LLM и возвращает ответ

---

### Итерация #3: Управление контекстом
**Цель:** Бот помнит историю диалога в рамках сессии

**Задачи:**
- [ ] Реализовать `ContextManager` класс
- [ ] Добавить формирование контекста из последних N сообщений
- [ ] Интегрировать `ContextManager` в обработку сообщений
- [ ] Ограничить контекст (max 10 сообщений)
- [ ] Добавить команду `/reset` для сброса контекста
- [ ] Написать тесты для `ContextManager` (добавление, ограничение, сброс)
- [ ] Написать тест для команды `/reset`
- [ ] Запустить тесты: `make test` - все должны пройти

**Тест:** Бот ведет связный диалог, учитывая предыдущие сообщения; `/reset` очищает историю

---

### Итерация #4: In-memory хранилище
**Цель:** Сохранять пользователей и диалоги в памяти

**Задачи:**
- [ ] Реализовать `MemoryStorage` класс
- [ ] Создать классы `User`, `Message`, `Conversation`
- [ ] Подключить сохранение пользователей при `/start`
- [ ] Сохранять историю диалогов в памяти
- [ ] Отслеживать метрики (количество пользователей, сообщений)
- [ ] Написать тесты для `MemoryStorage` (CRUD операции)
- [ ] Написать тесты для классов `User`, `Message`, `Conversation`
- [ ] Запустить тесты: `make test` - все должны пройти

**Тест:** После перезапуска бота данные теряются (expected), но в течение сессии история сохраняется

---

### Итерация #5: Логирование и обработка ошибок
**Цель:** Структурированное логирование и graceful обработка ошибок

**Задачи:**
- [ ] Настроить `structlog` с форматом `|` и файлами по дням
- [ ] Добавить логирование всех событий (сообщения, LLM-запросы, ошибки)
- [ ] Реализовать обработку ошибок LLM API
- [ ] Добавить обработку таймаутов и сетевых ошибок
- [ ] Отправлять пользователю понятные сообщения об ошибках
- [ ] Написать тесты для обработки ошибок (симуляция API ошибок)
- [ ] Проверить логирование в тестах
- [ ] Запустить тесты: `make test` - все должны пройти

**Тест:** При ошибке LLM бот не падает, логирует проблему в файл и сообщает пользователю

---

### Итерация #6: Полировка и оптимизация
**Цель:** Финальная проверка, тесты и документация

**Задачи:**
- [ ] Дополнить покрытие тестами для оставшихся компонентов
- [ ] Проверить все команды и сценарии работы вручную
- [ ] Обновить `README.md` с инструкцией по запуску
- [ ] Проверить обработку edge cases
- [ ] Оптимизировать производительность (если нужно)
- [ ] Провести интеграционное тестирование всего бота
- [ ] Запустить финальный `make test` - все должны пройти
- [ ] Проверить покрытие кода тестами

**Тест:** Все тесты проходят (`make test`), бот стабильно работает со всем функционалом

---

## 📝 Примечания

- Каждая итерация должна заканчиваться **работающим ботом**
- После завершения итерации обновляйте таблицу прогресса
- При возникновении проблем отмечайте статус ⚠️ и описывайте в комментариях
- Следуйте принципу KISS - если можно проще, делайте проще

