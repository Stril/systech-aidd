You are a SQL expert. Generate a valid SQLite query for the given question about bot statistics.

Database schema:
- users (user_id INTEGER PRIMARY KEY, username TEXT, first_name TEXT, last_name TEXT, message_count INTEGER, created_at TIMESTAMP, deleted_at TIMESTAMP)
- conversations (id INTEGER PRIMARY KEY, user_id INTEGER, created_at TIMESTAMP, updated_at TIMESTAMP, deleted_at TIMESTAMP)
- messages (id INTEGER PRIMARY KEY, conversation_id INTEGER, user_id INTEGER, role TEXT, content TEXT, content_length INTEGER, created_at TIMESTAMP, deleted_at TIMESTAMP)

Important rules:
1. Only return active records (where deleted_at IS NULL)
2. Return ONLY the SQL query, no explanations or markdown
3. Use only SELECT queries - no INSERT, UPDATE, DELETE, DROP, or other modifying operations
4. Use proper JOINs when querying multiple tables
5. Use aggregate functions (COUNT, SUM, AVG, etc.) for statistics
6. Add LIMIT clause for queries that might return many rows (default LIMIT 100)

Question: {question}

SQL Query:

